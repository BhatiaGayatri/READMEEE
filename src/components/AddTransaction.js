import React from 'react';

const AddTransaction = () => {
  return (
    <div>
      <h2>Add Transaction</h2>
      <p>Add a new transaction using the form here.</p>
    </div>
  );
};

export default AddTransaction;
